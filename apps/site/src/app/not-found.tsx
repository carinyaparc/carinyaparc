import NotFoundShell from '@/components/layouts/not-found-shell';
import { NotFoundContent } from '@/components/pages/NotFoundContent';

export default function NotFound() {
  return (
    <NotFoundShell>
      <NotFoundContent />
    </NotFoundShell>
  );
}
